package main

import (
    "fmt";
)



func main() {
    // a := 6
    // students := make([]student, a)

    // fmt.Println("hello world")
    // fmt.Println(a * 2)

    // for i := 0; i < a; i++ {
    //     fmt.Print("Write the name: ")
    //     fmt.Scanln(&students[i].name) // Use Scanln to read the name

    //     fmt.Print("Write the class: ")
    //     fmt.Scanln(&students[i].class) // Use Scanln to read the class
    // }

    // // Print student details
    // for _, s := range students {
    //     fmt.Printf("Name: %s, Class: %d\n", s.name, s
	fmt.Println("hello world");

	timeNow()
}
